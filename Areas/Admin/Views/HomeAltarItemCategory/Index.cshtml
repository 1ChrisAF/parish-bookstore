@model IEnumerable<parish_bookstore.Models.HomeAltarItemCategory>

@{
    ViewData["Title"] = "Altar Item Categories";
    ViewData["Header"] = "Home Altar Item Categories";
}

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    @if (item.HomeAltarItemCategoryId != 1)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HomeAltarItemCategoryId">Edit</a> | 
                <a asp-action="Delete" asp-route-id="@item.HomeAltarItemCategoryId">Delete</a>
            </td>
        </tr>
    }
}   
    </tbody>
</table>
