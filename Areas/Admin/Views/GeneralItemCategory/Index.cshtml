@model IEnumerable<parish_bookstore.Models.GeneralItemCategory>

@{
    ViewData["Title"] = "General Item Categories";
    ViewData["Header"] = "General Item Categories";
}

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    @if (item.GeneralItemCategoryId != 1)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GeneralItemCategoryId">Edit</a> | 
                <a asp-action="Delete" asp-route-id="@item.GeneralItemCategoryId">Delete</a>
            </td>
        </tr>
    }
}   
    </tbody>
</table>
